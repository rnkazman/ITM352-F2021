/* deposit = function(amount) { // can also do function deposit(amount)
    console.log(balance);
    balance += amount; 
    console.log("New balance is " + balance);
}

var balance = 600
deposit (50); // tried to do var deposit = 50; but that did not work - is it because deposit is already assigned to function?
console.log("Balance is " + balance);
*/ 